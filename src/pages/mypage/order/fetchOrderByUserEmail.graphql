# 注文履歴を取得
query SearchOrderByUserEmail($email: String) {
  order(email: $email) {
    id
    updatedAt
    item {
      id
      name
      image
      ingredients {
        name
      }
    }
    user {
      id
      email
    }
  }
}
